server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/islittlechen/webintegration.git
          label: web-integration-dev
          basedir: src/main/resources/api-integration-config
          username: islittlechen
          password: ilc227610
